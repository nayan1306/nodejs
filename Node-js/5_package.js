// To inintialize a modules folder and package.json file
// cmd: npm init

// To add a npm pacakage named colors
// cmd: npm i colors

const color = require('colors');
console.log("Package.json".red)

// Node-js is single threaded 
// i.e at a time it runs only a single command

// If node_modules file is deleted then  => cmd: npm install
// when you run npm install then all the dependencies are automatically installed
